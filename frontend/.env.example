# Frontend Environment Variables

# Backend API URL
# Leave empty to use same origin (recommended for production)
# Set to backend URL for development (e.g., http://localhost:3000)
VITE_API_URL=

# Keycloak OIDC Configuration (optional, only if using Keycloak)
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_CLIENT_ID=rpg-music
VITE_KEYCLOAK_REDIRECT_URI=http://localhost:5173/callback

# WebSocket URL
# Leave empty to automatically use current page origin (recommended)
# For production: will use https://your-domain.com
# For development: set to ws://localhost:3000
VITE_WS_URL=

# Example for local development:
# VITE_API_URL=http://localhost:3000
# VITE_WS_URL=http://localhost:3000

# How many rooms to create
ROOM_COUNT=3 # default to 5, WIP